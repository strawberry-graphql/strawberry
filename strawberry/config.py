from enum import IntEnum

# class EnumGenerate(IntEnum):
#     MEMBER = 0
#     VALUE  = 1

class StrawberryConfig:
    auto_camelcase: bool = True
    # resolvers_strict_mode: bool = True
    # enum_values = EnumGenerate.MEMBER # or VALUE
